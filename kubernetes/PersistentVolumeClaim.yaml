apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-PVC
  namespace: wanderlust
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  resources:
    requests:
      storage: 5Gi